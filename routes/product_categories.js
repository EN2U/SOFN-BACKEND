const express = require('express')
const userController = require('../controller/product_categories')
const auth = require('../middleware/auth')

const router = express.Router()

module.exports = router
