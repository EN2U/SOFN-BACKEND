const express = require('express')
const userController = require('../controller/enterprise_address')
const auth = require('../middleware/auth')

const router = express.Router()

module.exports = router
